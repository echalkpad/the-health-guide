/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GroupPipe } from './group.pipe';

describe('Pipe: Group', () => {
  it('create an instance', () => {
    let pipe = new GroupPipe();
    expect(pipe).toBeTruthy();
  });
});
