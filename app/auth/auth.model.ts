export class Auth {
    constructor(
        public id: string = "0",
        public avatar: string = "user.png",
        public name: string = "user",
        public email: string = "user@domain.com"
    ) { }
}